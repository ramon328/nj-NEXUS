import DashboardLayout from '@/components/DashboardLayout';
import { GaiaWorkspace } from '@/components/gaia-canvas/GaiaWorkspace';

const GaiaCanvas = () => {
  return (
    <DashboardLayout>
      <GaiaWorkspace />
    </DashboardLayout>
  );
};

export default GaiaCanvas;
