// This hook is now just a re-export of the context hook
// Kept for backwards compatibility
export { useAIChatContext as useAIChat } from '@/contexts/AIChatContext';
export type { ChatMessage, DashboardContext } from '@/contexts/AIChatContext';
