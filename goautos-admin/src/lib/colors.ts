import colors from 'tailwindcss/colors';

/** Primary brand color — single source of truth for JS contexts (gradients, inline styles, etc.) */
export const PRIMARY_COLOR = colors.sky[400]; // '#38bdf8'
