import Dashboard from "@/components/Dashboard";

// Página principal: el dashboard es interactivo, así que vive en un
// componente cliente separado.
export default function Home() {
  return <Dashboard />;
}
